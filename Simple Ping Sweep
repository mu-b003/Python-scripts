import os

network = "192.168.1."

print("Scanning network...\n")

for i in range(1, 255):
    ip = network + str(i)

    response = os.system(f"ping -c 1 -W 1 {ip} > /dev/null 2>&1")

    if response == 0:
        print(f"[+] {ip} is LIVE")
